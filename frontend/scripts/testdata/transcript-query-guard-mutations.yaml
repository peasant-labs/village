mutations:
  - name: response-pagination-guard-bypass
    file: ../../src/lib/queries/transcripts.ts
    original: "      assertTranscriptListResponseMatchesRequest(params, response);\n      if (options?.requireHarnessFacets) assertHarnessFacets(response);\n      return response;"
    replacement: "      if (options?.requireHarnessFacets) assertHarnessFacets(response);\n      return response;"
    testFile: src/mountedSessionListPagination.test.tsx
    testName: rejected-mismatch-then-new-page-keeps-last-confirmed-rows
